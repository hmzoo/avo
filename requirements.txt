torch==2.5.1
torchaudio==2.5.1
TTS>=0.22.0; python_version<'3.12'
transformers==4.38.2
networkx>=2.5.0,<3.0.0
flask>=3.0.0
flask-socketio>=5.3.0
requests>=2.31.0
python-socketio>=5.10.0
pillow>=10.0.0
numpy>=1.24.0

# Note: TTS (Coqui TTS) nécessite Python 3.9, 3.10 ou 3.11
# torch doit être < 2.6 (PyTorch 2.6+ a changé torch.load weights_only=True par défaut)
# transformers doit être < 4.40 pour la compatibilité avec TTS/XTTS
# networkx < 3.0 pour compatibilité avec gruut
# Pour Python 3.12+, utilisez une alternative ou installez Python 3.11
